var section = '<%= section %>';
if (section == 'requests') {
  $('.friends-content').html("<%= j(render 'requests_section') %>");
} else {
  $('.friends-content').html("<%= j(render 'friends_section') %>");
}
